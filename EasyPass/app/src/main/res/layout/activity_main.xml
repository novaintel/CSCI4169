<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:layout_marginBottom="0dp"
    android:layout_marginEnd="0dp"
    android:layout_marginLeft="0dp"
    android:layout_marginRight="0dp"
    android:layout_marginStart="0dp"
    android:layout_marginTop="0dp"
    android:background="#333333"
    tools:context="com.csci.easypass.MainActivity"
    tools:ignore="MergeRootFrame" >

        <requestFocus />

        <EditText
            android:id="@+id/firstpage_username"
            android:layout_width="320dp"
            android:layout_height="wrap_content"
            android:layout_above="@+id/firstpage_password"
            android:layout_centerHorizontal="true"
            android:layout_marginBottom="24dp"
            android:ems="10"
            android:hint="@string/username"
            android:inputType="textPersonName" />

        <EditText
            android:id="@+id/firstpage_password"
            android:layout_width="320dp"
            android:layout_height="wrap_content"
            android:layout_above="@+id/create_account_btn"
            android:layout_centerHorizontal="true"
            android:layout_marginBottom="24dp"
            android:ems="10"
            android:hint="@string/password"
            android:inputType="textPassword" />

        <Button
            android:id="@+id/create_account_btn"
            style="?android:attr/buttonStyleSmall"
            android:layout_width="150dp"
            android:layout_height="48dp"
            android:layout_alignLeft="@+id/firstpage_username"
            android:layout_centerVertical="true"
            android:background="@drawable/n7design_titlebar"
            android:text="@string/create_account"
            android:textSize="16sp" />

        <Button
            android:id="@+id/login_btn"
            style="?android:attr/buttonStyleSmall"
            android:layout_width="150dp"
            android:layout_height="48dp"
            android:layout_alignRight="@+id/firstpage_username"
            android:layout_centerVertical="true"
            android:background="@drawable/n7design_titlebar"
            android:text="@string/login"
            android:textSize="16sp" />

        <ImageView
            android:id="@+id/logo"
            android:layout_width="320dp"
            android:layout_height="wrap_content"
            android:layout_above="@+id/firstpage_username"
            android:layout_alignLeft="@+id/firstpage_username"
            android:layout_centerHorizontal="true"
            android:contentDescription="@string/app_name"
            android:src="@drawable/logo" />

</RelativeLayout>
